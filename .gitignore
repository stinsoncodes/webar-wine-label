.DS_Store
._*
.Spotlight-V100
.Trashes

node_modules/
.vercel/

# OneDrive conflict copies
*conflicted copy*

# tools/verify-names.py writes its contact sheet into the CWD by default, and the
# documented invocation runs from here. It is a thing you LOOK at, not a record.
names.png

# PowerPoint slide exports — a reproducible intermediate, ~40MB.
# The print masters in source/labels/ supersede these.
19_Crimes_Quanta Edition_Front Label/

# Source artwork. Large (~100MB each) and carries full names and employer
# branding. The print masters in source/labels/ are the committed record.
*.pptx

# The designer pack is a build artifact of tools/make-qr.py; regenerate it
# rather than versioning a zip.
source/qr-codes-for-designer.zip
